name: pavol-brain
services:
  falkordb:
    image: falkordb/falkordb:v4.14.9
    ports: ["6379:6379"]
    volumes: ["falkordb-data:/data"]
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep PONG"]
      interval: 5s
      timeout: 3s
      retries: 20
volumes:
  falkordb-data:
